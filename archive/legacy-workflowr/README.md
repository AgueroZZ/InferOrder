# Legacy workflowr archive

This directory retains the tracked InferOrder analysis sources from the
Smooth-EM era. They are historical records outside the `analysis/` and
`docs/` publication directories, so routine current-site builds do not
recreate obsolete public pages.
